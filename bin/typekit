#!/usr/bin/env ruby
lib = File.expand_path('../../lib', __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)

require 'typekit'

exit unless Typekit::Authentication.authenticated? || Typekit::Authentication.prompt_for_token

Typekit::CLI.start(ARGV)
